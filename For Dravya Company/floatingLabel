<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Floating Label</title>
    <style type="text/css">
        body {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-color: #f4f4f4;
    margin: 0;
}

.input-container {
    position: relative;
    margin: 20px;
    width: 200px;
}

input {
    width: 100%;
    padding: 10px;
    font-size: 16px;
    border: 1px solid #ccc;
    border-radius: 5px;
    outline: none;
}

label {
    position: absolute;
    top: 50%;
    left: 10px;
    font-size: 16px;
    color: #999;
    transition: all 0.3s ease;
    transform: translateY(-50%);
    pointer-events: none;
    background-color: white; /* To cover input's border */
    padding: 0 4px; /* Adds some padding to the label */
}

input:focus + label,
input:not(:placeholder-shown) + label {
    top: -10px;
    left: 5px;
    font-size: 12px;
    color: #333;
    transform: translateY(0);
}

    </style>
</head>
<body>
    <div class="input-container">
        <input type="text" id="amount" placeholder="AMOUNT" required>
        <label for="amount">AMOUNT</label>
    </div>
</body>
</html>
